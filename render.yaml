services:
  - type: web
    name: emojiify
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -b 0.0.0.0:5000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
